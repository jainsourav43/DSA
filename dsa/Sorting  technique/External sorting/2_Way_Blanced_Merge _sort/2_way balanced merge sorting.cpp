//2 way balanced merge sort where partition size =4
#include<bits/stdc++.h>
#define m 2
using namespace std;
int main()
{
 	int arr[4],temp1[4],temp2[4],size=4,flag=0,i,j=0;
 	cout<<"Enter the Size of partition \n";
 	cin>>size;
 	char str[50];
 	ifstream fin1,fin2,fin3;
 	fin1.open();
 	while(fin1.eof())
 	{
	 	for(i=0;i<size;i++)
	 	{
	 	  fin1>>a[i];
		} 
		sort(a,a+size);
		if(j%m==0)
		{
			
		}
		j++;
    }
}
